import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#1B5E20',
    },
    secondary: {
      main: '#D50000',
    },
    background: {
      default: '#FFF',
    },
  },
});

export default theme;
